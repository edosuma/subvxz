#!/bin/bash

git config --global user.email "101666083+vxzdev@users.noreply.github.com"
git config --global user.name "VxzDev"
git add .
git commit -am "Make it better"
git push origin master --force
